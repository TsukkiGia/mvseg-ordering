version: 1

# Fixed-uncertainty start-nomination simulation — ACDC
#
# Run:
#   python -m experiments.tools.start_nomination_launcher --config experiments/recipes/fixed_uncertainty_sim/acdc_fixed_uncertainty_sim.yaml --run

defaults:
  selectors:
    - closest_centroid
    - medoid
    - farthest_centroid
    - max_mean_distance
    - minimax_center

  encoders:
    - experiments/encoder_configs/clip_default.yaml
    - experiments/encoder_configs/vit_default.yaml
    - experiments/encoder_configs/dinov2_default.yaml
    - experiments/encoder_configs/medsam_default.yaml
    - experiments/encoder_configs/medsam_spatial_mean.yaml
    - experiments/encoder_configs/multiverseg_default.yaml
    - experiments/encoder_configs/multiverseg_spatial_mean.yaml

  procedure: random_vs_uncertainty_final
  ablation: pretrained_baseline5p
  dataset: ACDC
  random_policy: random
  data_split: train
  dataset_seed: 42
  device: cpu

simulations:
  - name: acdc_curriculum
    policies:
      - curriculum
    out_dir: experiments/scripts/fixed_uncertainty_sim_final/acdc/curriculum
  - name: acdc_reverse_curriculum
    policies:
      - reverse_curriculum
    out_dir: experiments/scripts/fixed_uncertainty_sim_final/acdc/reverse_curriculum